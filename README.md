# Виджет банковских операций

Проект представляет собой набор утилит для фильтрации и сортировки транзакций личного кабинета клиента.

## Описание функций модуля `processing`

В модуле `src/processing.py` реализованы следующие функции для обработки списка словарей с данными:

### 1. `filter_by_state(data, state='EXECUTED')`
Фильтрует список банковских операций по их статусу.
* **Вход:** Список словарей с операциями и строковое значение статуса.
* **Выход:** Новый список операций с требуемым статусом.

### 2. `sort_by_date(data, reverse=True)`
Сортирует список банковских операций по дате.
* **Вход:** Список словарей с операциями и флаг порядка сортировки.
* **Выход:** Новый отсортированный список операций.

## Инструкция по установке и использованию

1. Клонируйте репозиторий на свой ПК.
2. Установите зависимости проекта с помощью `poetry install`.
3. Импортируйте нужные функции в свой код:

```python
from src.processing import filter_by_state, sort_by_date

# Пример фильтрации
executed_ops = filter_by_state(data, 'EXECUTED')

# Пример сортировки
sorted_ops = sort_by_date(data)